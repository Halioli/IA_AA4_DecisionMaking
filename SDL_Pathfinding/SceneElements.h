#pragma once

enum Color { White = 0, Black, Red, Orange, Yellow, Green, Blue, Purple };

enum SceneElements {
	WhiteKey = 0, 
	BlackKey,
	RedKey,
	OrangeKey,
	YellowKey,
	GreenKey,
	BlueKey,
	PurpleKey,
	Coin,
	Count
};
